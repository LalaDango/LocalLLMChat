# 検証済みモデル一覧（LocalLLMChat向け縮約版）

最終更新: 2026-07-07

> モデル本体の性質（速度・checkpoint挙動・位置参照限界・JSON/tool安定性）はFLM直結クライアント
> （LocalLLMChat等）にも当てはまる。「SM運用」由来の対策系（脱線対策等）はLocalLLMChatには未実装・無関係。

## FastFlowLM (NPU実行)

### ✅ gemma4-it:e4b — 現在の主力モデル

| 項目 | 値 | 検証日 |
|------|------|--------|
| ステータス | **主力運用中** | 2026-06〜 |
| アーキテクチャ | Gemma 4 E4B (MatFormer)・hybrid attention（KV抑長型） | - |
| 起動構成 | ctx-len 32768 ＋ --pmode turbo ＋ --prefill-chunk-len 4096 | 2026-07-05 |
| prefill速度 | 429〜458tps（高占有帯でも崖なし） | 2026-06-11 |
| decode速度 | 7.40tps(0-10%帯) → 5.34tps(90-100%帯)・単調低下で崖なし | 2026-07-04 |
| checkpoint挙動 | **生成後にcheckpointを保持** → 次ターンは新規userトークン分のみprefill。**16K級の添付文書ありでも成立**（差分12tok/TTFT1.5s、LocalLLMChat直結で確認） | 2026-07-06 |
| 品質 | メタ分析タスクで9B同等以上の印象。ニュース平易化等で常用実績 | 2026-06-11 |
| JSON/tool出力 | ⚠️ OpenAI形式の素のツールループは不可（role:"tool"をFLMテンプレートが落とす）→ **LocalLLMChatはrole:tool→user変換（v2）で解決済み**。結果追従はdatetime 5/5・クイズ実質4/5、発火はツール名明示で100%（known-issues.md「ツール連携」参照） | 2026-07-07 |
| thinking制御 | **reasoning_effort はGemma系では無視される**（qwen3/qwen3.5系限定の機能）。e4bのthinkingは**プロンプト冒頭の `<\|think\|>` トークンでON/OFF**（例: ユーザー質問の冒頭に付けるだけで発火することを実機確認）。**effortのような段階指定は不可** | 2026-07-06 |
| 30K級長文占有 | 91%超まで運用実績 | 2026-07-04 |
| 位置参照（添付文書） | **16K tok（60KB級・占有49%）で8/8満点**。先頭・末尾・横断・ネガティブコントロール全通過。末尾の途中切れ検出＋16K地点の一字一句引用まで確認（LocalLLMChat経由） | 2026-07-06 |
| 添付文書のTTFT | 16K tok全量prefill 28〜35s（563tps）。文書固定後のフォローアップは差分prefill（12tokで TTFT 1.5s） | 2026-07-06 |
| Vision対応 | **✅ 動作確認済み**（FLM v0.9.43）。`vision_weight.q4nx`（912MB）同梱・`model_list.json` で `vlm: true`。図形・文字・レシートOCRを正確に処理。7リクエスト連続でflmプロセス+28MBのみ、クラッシュなし、decode ~7tps（テキスト時と同等）、TTFT 2.5〜8.7s | 2026-07-06 |
| 画像のKVコスト | **1枚 ≒ 256トークン固定**。エンコーダが解像度によらず固定サイズに正規化（1024px版と2048px版で prompt_tokens 完全一致を3画像ペアで確認）→ 高解像度で送っても追加画素はモデルに届かない。**送信は長辺1024px・JPEG quality 85で十分**（LocalLLMChatの `FileProcessor.kt` 準拠。2048px/q95へのA/Bは品質ゲインゼロで差し戻し済み） | 2026-07-06 |

#### e4bの挙動特性（モデル特性なのでLocalLLMChatにも示唆あり）

- **位置指定はとり違え・名指しは当たる**: 高占有帯（72/82/90%で実証）で「直近3件」等の
  位置参照は毎回とり違え。同じ内容をタイトルで名指しすると全問正解。
  「忘れるのではなく、数えられない」（SWA512の遠距離位置分解能限界説が有力）
- **ただし添付文書内の位置参照は16K/49%占有まで無傷**（2026-07-06、8/8満点）:
  「とり違え」は高占有域（72%超）＋**会話履歴の件数カウント**（「直近3件」等）の現象で、
  **単一文書内の位置参照とは別物**。文書内なら先頭・末尾・離れた2箇所の横断まで正確。
  劣化開始帯は占有49%〜72%の間のどこか（未検証）。劣化はまず「近くの別の数値との取り違え」として
  出る想定なので、誤答時はどの数字を持ってきたかで劣化帯域を推定できる
- **画像内文字の読解限界は原寸12〜14px相当**（解像度ラダー実測）。レシートの12px fine printまでは
  満点で読めるが、**境界サイズでは「読めた風」の幻覚コードを出す**（テスト字種に無いはずの文字を
  出力した実績あり）。完全に読めない場合は正直にunreadableと言うので、**半端に潰れた文字が一番危ない**。
  画像OCRの数値をそのまま信用しないこと
- グラフの数値読み取りは~50%程度で**系列取り違え癖**あり（解像度と無関係のモデル特性）。
  表・レシートのような整列テキストは得意、プロット上の値対応付けは苦手

### ⚠️ qwen3.5:9b テキスト専用改造 — 控え（e4b主軸化により後備）

| 項目 | 値 | 検証日 |
|------|------|--------|
| 改造内容 | config.jsonからVision参照削除。vision_weight.q4nx (~636MB) 不使用 | 2026-03-27 |
| コンテキスト | ctx-len 16384 | - |
| 旧1,792tok prefill上限 | **v0.9.43で消滅**（chunk prefillによる） | 2026-06-11 |
| decode速度 | ~7.8-9.2 tok/s（天井付近でも劣化ほぼなし・Gated DeltaNetの恩恵） | 2026-03-30 |
| checkpoint挙動 | **生成後checkpointを持たない** → 毎ターン差分prefill用 | 2026-06-12 |
| reasoning_effort | none=思考ゼロで完全動作。**mediumは思考暴走リスク**（max_tokensを全部thinkingに消費→content空・finish_reason=length） | 2026-06-11 |

### ✅ Gemma 4 E2B — 検証済み（v0.9.39時点）。現在はe4bが主力のため参考扱い

### 🟦 Qwen3.5:4b — 旧主力（退役）

- 2026-03期の主力。ctx 32,768、実用文書上限28KB目安（LocalLLMChatのMAX_TEXT_BYTES較正の由来）
- e4b主軸化に伴い退役。**qwen3-it:4bは検索AI Vane用に現役**（ツール呼び出し・JSON出力が安定）

### 🟦 Qwen3-8B (Q4_1) — 旧々主力（退役）

- ~5GB、ctx 8,192、decode ~10.7 tok/s、従来型Transformer

### ❌ Qwen3.5:9b 公式マルチモーダル版 — 動作不可

- Vision込みでウェイトが7.6GB超 → XRTエラーで起動不可（ctx-len 128でも不可）。2026-03-27検証

## 新モデル評価テンプレート

新しいモデルについて聞かれたら以下を確認:

```
□ パラメータ数: ___B
□ 量子化後サイズ (Q4系): ___GB
□ 7.6GB以内か: YES / NO
□ FastFlowLM対応アーキテクチャか: YES / NO / 未確認
□ NPU最適化フォーマット有無: YES / NO / 未確認
□ 想定コンテキスト長: ___トークン
□ KVキャッシュ見積り: ___GB
□ モデル + KVキャッシュ合計: ___GB < 7.6GB: YES / NO
□ checkpoint挙動: 生成後保持型(e4b型) / 非保持型(9B型) / 未確認
→ 判定: ✅動作可能 / ⚠️条件付き / ❌不可 / ⏳情報不足
```
